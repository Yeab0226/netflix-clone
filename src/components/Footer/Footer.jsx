import styles from "./Footer.module.css";

function Footer() {
  return (
    <footer className={styles.footer}>
      {/* T-09: Build the responsive footer here. */}
    </footer>
  );
}

export default Footer;
